#include <stdio.h>
/**
 * main - evaluates an integer
 * Return: zero if excuted properly
 */
int main(void)
{
	char pu[] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r', '\n'};
	
	for (int i = 0; i <= 8; i++)
		putchar(pu[i]);	
	return (0);
}
