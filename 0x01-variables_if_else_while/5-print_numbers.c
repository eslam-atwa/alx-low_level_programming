#include <stdio.h>

/**
* main - The start and end function prints numbers
* Return: 0 in if successful
*/
int main(void)
{
	int m;

	for (m = 0; m <= 9; m++)
		putchar(m);
	putchar('\n');
	return (0);
}
